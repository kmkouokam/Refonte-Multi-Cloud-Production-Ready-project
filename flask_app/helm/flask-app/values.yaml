replicaCount: 1

image:
  repository: "kmkouokam/flaskr-tdd"
  tag: "1.0.0"
  pullPolicy: "IfNotPresent"

service:
  type: ClusterIP
  port: 5000

resources:
  requests:
    cpu: 300m
    memory: 300Mi
  limits:
    cpu: 600m
    memory: 600Mi

env:
  FLASK_ENV: production

db:
  secretName: "flask-db-secret"
  # database_url: "postgresql://flaskuser:<SECRET_FROM_AWS_OR_GCP>@refonte-flask-db.devopsguru.today:5432/flaskr"
  # host: "refonte-flask-db.devopsguru.today"
  # port: "5432"
  # name: "flaskr"
  # user: "flaskuser"
  # password: "<SECRET_FROM_AWS_OR_GCP>"

ingress:
  enabled: false
  host: ""
  annotations: {}
